# 启动端口
server:
  port: 8088
  # 访问路径前缀
  servlet:
    context-path: /
spring:
  # 德鲁伊数据源
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/service-plarform
    username: root
    password: xiaoyu